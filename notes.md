# Notes for learning streamlit framework for the data web app using python script. 
- `python -m streamlit run test.py` to launch the web app. 
- `streamlit run https://test.py` to launch from github scripts. 
